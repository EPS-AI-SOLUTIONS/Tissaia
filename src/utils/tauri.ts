// src/utils/tauri.ts
/**
 * Tauri Detection — v4.0 Web Edition (STUB)
 * ==========================================
 * Tissaia v4.0 is a pure web application.
 * This module is kept for backward compatibility but always returns false.
 * @deprecated Will be removed in v5.0
 */

/**
 * @deprecated Always returns false in v4.0 Web Edition.
 */
export function isTauri(): boolean {
  return false;
}
