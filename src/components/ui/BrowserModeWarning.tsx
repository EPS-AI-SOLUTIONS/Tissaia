// src/components/ui/BrowserModeWarning.tsx
/**
 * Browser Mode Warning Banner — v4.0 Web Edition (NOOP)
 * ======================================================
 * In v4.0 Web Edition, Tissaia always runs in the browser.
 * This component is kept as a no-op for backward compatibility.
 * @deprecated Will be removed in v5.0
 */

export default function BrowserModeWarning() {
  // v4.0 is always web — no warning needed
  return null;
}
